# AI 协作模式 V4 稳定主规范

V4 是当前稳定主链路。它强调四件套分工、GitHub 事实源、任务文件驱动和验收闭环。

## 一、角色分工

### ChatGPT：总控 / 架构判断 / 任务包 / 验收

ChatGPT 负责与用户充分沟通，判断方向，做取舍，拆任务，写任务文件，并在执行报告回来后验收。ChatGPT 不直接替代事实源，也不在未读取 GitHub 的情况下判断项目当前状态。

### GitHub：唯一事实源 / 项目状态机 / 留痕系统

GitHub 保存项目当前状态、任务、决策、执行报告、验收记录和关键文档。任何执行任务都必须以 GitHub 中的任务文件为准，任何验收都必须回到 GitHub 中核验。

### Codex：现场交付负责人 / 最终集成者 / 报告提交者

Codex 负责读任务、读项目、组织执行、改文件、跑测试、整合结果、提交报告。Codex 可以调用本地工具或辅助分析，但必须对最终 diff、测试和报告负责。

### Claude Code：本地工程增强工具 / 深度代码分析 / 局部修复 / 复审

Claude Code 用于工程增强：长上下文代码阅读、调用链分析、复杂错误定位、局部修复草案和复审。Claude Code 不直接替代 Codex，不独立决定发布或部署。

## 二、核心流程

1. 用户在 ChatGPT 中明确说“进入执行”或“可以安排 Codex”。
2. ChatGPT 读取 GitHub 事实源，生成任务文件或更新任务文件。
3. 任务文件写入 GitHub，成为 Codex 的执行依据。
4. Codex 按任务文件执行，必要时组织 Claude Code 做只读分析或局部复审。
5. Codex 完成后把执行报告写回 GitHub。
6. ChatGPT 读取 GitHub 中的报告、diff、测试结果与项目状态，给出 PASS / PARTIAL PASS / FAIL / BLOCKED 验收结论。
7. 若未通过，ChatGPT 生成下一轮任务包，而不是只在聊天中继续口头修补。

## 三、任务文件最低标准

任务文件必须包含：

- 任务名
- 背景
- 目标
- 范围
- 禁止事项
- 执行步骤
- 验收标准
- 报告要求
- 停止条件
- 下一步预案

## 四、禁止行为

- 不读 GitHub 就判断项目状态。
- 只在聊天里派活，不落任务文件。
- Codex 没有任务文件就直接改业务代码。
- Claude Code 独立替代 Codex 做最终集成。
- 把实验室能力直接当稳定模块使用。
- 跨项目混入文件、记忆、配置或任务。
- 未经授权部署、改数据库、改密钥或改生产自动化。

## 五、验收口径

- PASS：目标完成，测试或检查通过，没有触碰禁止范围。
- PARTIAL PASS：主要目标完成，但存在明确剩余项、外部阻塞或未覆盖验证。
- FAIL：目标未完成，或关键检查失败，或触碰禁止范围。
- BLOCKED：受权限、依赖、环境、凭证或外部条件阻塞，无法继续安全执行。

V4 的核心不是“让 AI 多干活”，而是让 AI 的讨论、执行、报告、验收都围绕同一个事实源闭环。
