# Indexes

Auto-generated indexes:

- `file_manifest.json` — all project files with size and suffix
- `code_index.jsonl` — source code files only
- `report_index.jsonl` — report files with title and size
- `decision_index.jsonl` — decision files with title and size
- `project_brief.md` — brief project summary for context packs
