# modules

`modules/` 用于存放已经通过验证、可以复用到多个项目的稳定协作模块。

## 收录标准

进入 `modules/` 的内容必须满足：

- 已在 `lab/` 或真实项目中完成验证。
- 有明确适用范围。
- 有明确输入、输出、禁止事项和验收标准。
- 不依赖某个单一项目的私有上下文。
- 不包含密钥、生产配置或业务项目代码。

## 当前状态

当前暂不把实验室内容升级为稳定模块。V4 主链路仍以 `AI_COLLABORATION_MODE_V4.md` 为准。
